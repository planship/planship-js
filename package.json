{
  "name": "planship-js",
  "version": "0.3.0",
  "description": "",
  "private": true,
  "keywords": [],
  "author": "pawel@planship.io",
  "license": "MIT",
  "scripts": {
    "build": "pnpm -r build",
    "openapi-gen": "pnpm -r openapi-gen",
    "build-docs": "pnpm -r build-docs",
    "publish-alpha": "pnpm -r publish --tag alpha --no-git-checks",
    "format": "pnpm -r format"
  }
}
